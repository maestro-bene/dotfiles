#!/bin/sh

if [ ! -f "/home/maestro-bene/.config/chezmoi/key.txt" ]; then
    mkdir -p "/home/maestro-bene/.config/chezmoi"
    chezmoi age decrypt --output "/home/maestro-bene/.config/chezmoi/key.txt" --passphrase "{{ .chezmoi.sourceDir }}/key.txt.age"
    chmod 600 "/home/maestro-bene/.config/chezmoi/key.txt"
fi
